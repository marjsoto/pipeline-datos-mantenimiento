# Conexion a la base del Proyecto 1 (el contenedor Docker maintenance_db)
DATABASE_URL=postgresql+psycopg://maintenance_user:changeme@localhost:5432/maintenance_db

# Carpeta donde el pipeline escribe los reportes generados
OUTPUT_DIR=output
